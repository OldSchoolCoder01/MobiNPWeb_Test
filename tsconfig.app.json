{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": ["node"]
  },
  "files": [
    "src/main.ts",
    "src/polyfills.ts"    // Add polyfills.ts
  ],
  "include": [
    "src/**/*.d.ts",
    "src/**/*.ts"         // Ensure all TypeScript files in src directory are included
  ]
}
